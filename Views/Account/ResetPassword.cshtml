@model Report_a_Fault.ViewModel.ResetPasswordVM

@{
    ViewData["Title"] = "Login";
    Layout = "_Layout";
}
@for (var i = 0; i < 7; i++)
{
    <div></div>
    <br />
}
<div class="background-container">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow border">
                <div class="card-body text-center">
                    <div class="row">
                        <div class="col-12 text-lg-start">
                            <img src="~/images/CUT.png" height="50px" />

                        </div>
                    </div>
                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input asp-for="Email" hidden class="form-control" aria-required="true" />
                        <input asp-for="Token" hidden class="form-control" aria-required="true" />
                        <br />
                        <div class="form-floating mb-3">
                            <input asp-for="Password" class="form-control" aria-required="true" />
                            <label asp-for="Password" class="form-label"></label>
                            <span asp-validation-for="Password" class="text-danger"></span>
                        </div>
                        <div class="form-floating mb-3">
                            <input asp-for="ConfirmPassword" class="form-control" aria-required="true" />
                            <label asp-for="ConfirmPassword" class="form-label"></label>
                            <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                        </div>

                        <div>
                            <button class="btn btn-form" asp-action="ResetPassword" asp-controller="Account" type="submit">Submit</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
